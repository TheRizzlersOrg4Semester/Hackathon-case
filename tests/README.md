# Test Strategy Scaffold

Test directories are split by intent:

- `tests/unit`: isolated domain and utility tests
- `tests/integration`: service + persistence behavior tests
- `tests/e2e`: browser flows for MVP user journeys

Feature-level tests will be added when each feature reaches implementation phase.



mathias er dum 
